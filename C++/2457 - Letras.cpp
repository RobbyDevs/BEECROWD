#include <bits/stdc++.h>
using namespace std;

int main(){
    string letra,palavra,v;
    long c;
    
    cin>>letra;
    vector <string> v;
    while(true){
        if(!(cin>>palavra)){
            break;
        }
        else{
            cin>>palavra;
        }
        v.push_back();
    }

    for (int i = 0; i < (v.size()); i++){

    }
}